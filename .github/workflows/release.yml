name: Release

on:
  release:
    types: [published]

jobs:
  release:
    permissions:
      id-token: write
      contents: write
      packages: write

    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: .nvmrc
          scope: '@joinmarket-webui'

      - name: Install
        run: npm clean-install

      - name: Build
        run: npm run build

      - name: Publish to npm Registry
        run: npm run publish:npm
        # disabled till npm 2FA can be done with TOTP (last checked on 2026-08-25)
        # Can "publish", but cannot "approve" or "reject" packages staged for publishing
        # via `npm stage approve|reject <stage-id>`
        if: false

      - name: Publish to Github Registry
        run: |
          npm set //npm.pkg.github.com/:_authToken ${{ secrets.GITHUB_TOKEN }}
          npm run publish:ghr
